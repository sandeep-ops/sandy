node {
        stage ( 'SCM' ) {
                // git clone
                git 'https://github.com/sandeep-ops/sandy.git'
        }

        stage ( 'build the packages' ) {
                // mvn package
                sh 'mvn package'

        }

    
}
